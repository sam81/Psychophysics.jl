language: julia
os:
    - linux
    - osx
julia:
    - release
    - nightly
notifications:
    email: false

after_success:
- julia -e 'cd(Pkg.dir("Psychophysics")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(process_folder())'